import * as React from "react";
import Search from "./Search";

function Header() {
  return (
    <header>
      <nav>
        <h1>This is the nav</h1>
      </nav>
      <Search />
    </header>
  );
}

export default Header;
